<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic only for local development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.32</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

    <!-- For production, all traffic should be over HTTPS -->
    <!-- Remove the domain-config above and ensure all APIs use HTTPS -->
</network-security-config>
